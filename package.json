{
  "name": "razorpay",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && pnpm run dev\" \"uvicorn backend.app.main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}